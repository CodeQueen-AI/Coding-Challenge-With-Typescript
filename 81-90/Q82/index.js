//Find the Length of a String: Write a function that takes a string as an argument and returns the number of 
//characters in the string.
function stringLength(str) {
    return str.length; // Returns the number of characters in the string
}
console.log(stringLength("Alice")); // Outputs: 5
